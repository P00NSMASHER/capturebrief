"""CaptureBrief external-provider adapters.

Adapters translate provider responses into canonical facts only. They do not make
pursuit decisions, silently reconcile conflicts, or retain credentials.
"""
